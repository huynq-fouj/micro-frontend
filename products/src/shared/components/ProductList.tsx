import { ProductType } from "../types/product";

export default function ProductList({ products }: Readonly<{ products: ProductType[] }>) {

    return (
        <div className="product-list-container">
            
        </div>
    )
}